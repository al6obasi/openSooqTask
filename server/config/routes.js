module.exports = function (app, express) {

 /*								Api Hotel route
 / ============================================================================== */
		app.get('/jers',function(req,res){
			res.json('hello word temp')
		});
	};